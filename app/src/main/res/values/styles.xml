<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.First_try" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.First_try" parent="Base.Theme.First_try" />

    <style name="LoginButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/login_button_color</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item>
    </style>
</resources>